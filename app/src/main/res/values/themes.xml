<resources>
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bottom_highlight</item>
        <item name="colorPrimaryVariant">@color/element_highlight</item>
        <item name="colorOnPrimary">@color/dark_text</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/element_highlight</item>
        <item name="colorSecondaryVariant">@color/bot_bar_background</item>
        <item name="colorOnSecondary">@color/dark_text</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/bottom_highlight</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MyApplication.Fullscreen" parent="Theme.MyApplication">
        <item name="android:actionBarStyle">@style/Widget.Theme.MyApplication.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.MyApplication.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

</resources>